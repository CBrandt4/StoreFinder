//import { getDistance, orderByDistance, isPointWithinRadius } from 'geolib';

// export function getDistances(
// places: any[],}
// s-haversin.distance or geoLib getDistance

// chose geoLib for simplicity, has more features, and users

//orderByDistance(point, arrayOfPoints)
// getDisance(start, end, accuracy)

//isPointwithinRadius(point, center, radius)

//ConvertDistance
//geolib.convertDistance(14200, 'mi'); // 14.2 //convert distance in meters to another unit
